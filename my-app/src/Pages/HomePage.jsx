import React from "react"
import axios from 'axios'

export default function HomePage(){
    React.useEffect(()=>{
        const FtchData=async()=>{
            const see={
            }
            try {
                
            } catch (error) {
                console.error(error)
            }
        }
        FtchData();
    },[])
    return <p>Homepage</p>
}
 